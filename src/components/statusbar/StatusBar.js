import React from "react";

import "./status-bar.scss";


const StatusBar = (props) => {
  return (
    <div className="row no-flex status-bar">
      <div>Ready</div>
    </div>
  );
};

export default StatusBar;
